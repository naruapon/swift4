import UIKit

let container = UIView(frame: CGRect(x: 0, y: 0, width: 375, height: 667))

let label = UILabel(frame: CGRect(x: 20, y: 20, width: 150, height: 30))
label.text = "Hello World"

container.addSubview(label)